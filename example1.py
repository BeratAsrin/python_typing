# Let us say we have a function that sums up two integers, therefore it returns also an integer.
def integer_sum(x: int, y: int) -> int:
    return x + y


result = integer_sum(5, 6)  # We are sending two integers to the function, and it returns us the integer result.

print(result)
